---

---

<section
    class="min-h-screen flex items-center justify-center bg-gray-50 py-10 px-4"
>
    <div class="bg-white shadow-xl rounded-3xl p-8 w-full max-w-md">
        <h1 class="text-2xl font-semibold text-center text-gray-800 mb-6">
            💳 Pago seguro
        </h1>

        <form id="payment-form" class="space-y-5">
            <!-- Información del comprador -->
            <div>
                <label class="block text-sm font-medium text-gray-600 mb-1"
                    >Nombre completo</label
                >
                <input
                    type="text"
                    class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                    placeholder="Sofía Large"
                    required
                />
            </div>

            <div>
                <label class="block text-sm font-medium text-gray-600 mb-1"
                    >Correo electrónico</label
                >
                <input
                    type="email"
                    class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                    placeholder="sofia@example.com"
                    required
                />
            </div>

            <!-- Datos de tarjeta -->
            <div class="mt-4 border-t pt-4">
                <label class="block text-sm font-medium text-gray-600 mb-1"
                    >Número de tarjeta</label
                >
                <input
                    type="text"
                    maxlength="19"
                    class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                    placeholder="1234 5678 9012 3456"
                    required
                />
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-sm font-medium text-gray-600 mb-1"
                        >Expira</label
                    >
                    <input
                        type="text"
                        maxlength="5"
                        class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                        placeholder="MM/AA"
                        required
                    />
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-600 mb-1"
                        >CVV</label
                    >
                    <input
                        type="text"
                        maxlength="3"
                        class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                        placeholder="123"
                        required
                    />
                </div>
            </div>

            <!-- Dirección -->
            <div class="mt-4 border-t pt-4">
                <label class="block text-sm font-medium text-gray-600 mb-1"
                    >Dirección</label
                >
                <input
                    type="text"
                    class="w-full border border-gray-300 rounded-xl p-2.5 focus:outline-none focus:ring-2 focus:ring-orange-500"
                    placeholder="Calle 123 #45-67, Santa Marta"
                    required
                />
            </div>

            <!-- Botón -->
            <button
                type="submit"
                class="w-full bg-orange-500 text-white font-semibold py-2.5 rounded-full hover:bg-orange-600 duration-300"
            >
                Pagar ahora
            </button>
        </form>

        <p
            id="success-msg"
            class="hidden text-green-600 text-center mt-4 font-medium"
        >
            ✅ Pago procesado correctamente (demo)
        </p>
    </div>
</section>

<script>
    if (typeof window !== "undefined") {
        const form = document.getElementById("payment-form");
        const successMsg = document.getElementById("success-msg");

        form.addEventListener("submit", (e) => {
            e.preventDefault();
            form.reset();
            successMsg.classList.remove("hidden");
            setTimeout(() => successMsg.classList.add("hidden"), 3000);
        });
    }
</script>
